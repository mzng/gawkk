<%= Time.now.strftime("%m/%d/%Y at %I:%M%p") %>

Images Backed Up to Amazon S3 Yesterday:

<% @files.each_with_index do |file, i| -%>
<%= Array.new(@files.size.to_s.length - (i + 1).to_s.length).collect{|j| ' '}.join %><%= i + 1 %>: <%= file %>
<% end -%>