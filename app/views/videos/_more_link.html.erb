<% container_id ||= '' -%>

| <%= link_to_remote 'More', :url => {:controller => 'videos', :action => 'share', :id => video, :container_id => container_id, :force => true}, :loading => "work('#{video.id}','#{container_id}');", :complete => "rest('#{video.id}','#{container_id}');" %><%= image_tag("menu-arrow#{open ? '' : '-up'}.png", :id => "more_link_arrow_for_#{video.id}#{container_id}", :class => 'pull') %>